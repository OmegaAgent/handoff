{
  "name": "@handoffproto/sdk",
  "version": "0.2.0",
  "description": "The TypeScript client for Handoff: one human answer, delivered to your agent exactly once, as typed data, authorizing exactly one effect.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22.18"
  },
  "keywords": [
    "handoff",
    "human-in-the-loop",
    "ai",
    "agents",
    "approvals",
    "protocol"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/OmegaAgent/handoff"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
